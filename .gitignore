# ══ HSMA ignore registry (CA-R196: the ignore list is itself documented) ══

# ── Regenerable runtime outputs: the generator and cmake reproduce these ──
build/
cmake-build-debug/
cmake-build-release/
*.d
*.slo
*.lo
*.o
*.obj
*.gch
*.pch
*.so
*.dylib
*.dll
*.mod
*.smod
*.lai
*.la
*.a
*.lib
*.exe
*.out
*.app

# ── Environment and editor noise ──
.vscode/
.idea/
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# ── Local-only artifacts, not part of the tracked record ──
# hsma_backup.tar.gz: the maintainer's local safety tarball (redundant to the repo)
hsma_backup.tar.gz
# hsma-core/: nested-clone defense (DEF-164 / DEC-247)
hsma-core/
# docs/*.bak.*: editor backup files
docs/*.bak.*
